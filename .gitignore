*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/hot
# Must never be tracked: git would check it out as a real directory and
# `php artisan storage:link` then refuses to replace it with the symlink.
/public/storage
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
